-- qvirthh Hub | PS99 - Made By qvirthh

print("qvirthh Hub | PS99 - Made By qvirthh")

local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/qvirthh/library.lua/refs/heads/main/library%20file"))()

if not Library then
    print("ERROR: Failed to load UI library!")
    return
end

local Window = Library:CreateWindow({
    Title = "qvirthh Hub | PS99",
    HubName = "qvirthh Hub",
    Theme = "Ocean",
    SizeUI = 1,
    DefaultToggleKey = Enum.KeyCode.LeftControl,
    UseKey = false
})

local AutoFarmTab = Window:CreateTab("Auto Farm")
local SettingsTab = Window:CreateTab("UI Setting")

local autoFarm = false
local autoCollect = false
local autoHatch = false
local autoMerchant = false
local autoEvent = false
local autoPlant = false
local autoPet = false
local autoRebirth = false
local antiAFK = false

AutoFarmTab:CreateToggle({
    Name = "⚡ Auto Farm",
    Default = false,
    Callback = function(state)
        autoFarm = state
        if state then
            task.spawn(function()
                while autoFarm and task.wait(0.1) do
                    pcall(function()
                        for _, v in pairs(workspace:GetDescendants()) do
                            if v:IsA("Model") and v:FindFirstChild("ClickDetector") then
                                fireclickdetector(v.ClickDetector)
                            end
                        end
                    end)
                end
            end)
        end
    end
})

AutoFarmTab:CreateToggle({
    Name = "💰 Auto Collect",
    Default = false,
    Callback = function(state)
        autoCollect = state
        if state then
            task.spawn(function()
                while autoCollect and task.wait(0.5) do
                    pcall(function()
                        local ReplicatedStorage = game:GetService("ReplicatedStorage")
                        for _, v in pairs(ReplicatedStorage:GetDescendants()) do
                            if v:IsA("RemoteEvent") and (v.Name:lower():find("collect") or v.Name:lower():find("claim")) then
                                v:FireServer()
                                break
                            end
                        end
                    end)
                end
            end)
        end
    end
})

AutoFarmTab:CreateToggle({
    Name = "🥚 Auto Hatch",
    Default = false,
    Callback = function(state)
        autoHatch = state
        if state then
            task.spawn(function()
                while autoHatch and task.wait(2) do
                    pcall(function()
                        local ReplicatedStorage = game:GetService("ReplicatedStorage")
                        for _, v in pairs(ReplicatedStorage:GetDescendants()) do
                            if v:IsA("RemoteEvent") and (v.Name:lower():find("hatch") or v.Name:lower():find("egg")) then
                                v:FireServer()
                                break
                            end
                        end
                    end)
                end
            end)
        end
    end
})

AutoFarmTab:CreateToggle({
    Name = "🛒 Auto Merchant",
    Default = false,
    Callback = function(state)
        autoMerchant = state
        if state then
            task.spawn(function()
                while autoMerchant and task.wait(3) do
                    pcall(function()
                        local ReplicatedStorage = game:GetService("ReplicatedStorage")
                        for _, v in pairs(ReplicatedStorage:GetDescendants()) do
                            if v:IsA("RemoteEvent") and (v.Name:lower():find("merchant") or v.Name:lower():find("shop") or v.Name:lower():find("buy")) then
                                v:FireServer("All")
                                break
                            end
                        end
                    end)
                end
            end)
        end
    end
})

AutoFarmTab:CreateToggle({
    Name = "🎪 Auto Join Event",
    Default = false,
    Callback = function(state)
        autoEvent = state
        if state then
            task.spawn(function()
                while autoEvent and task.wait(2) do
                    pcall(function()
                        local ReplicatedStorage = game:GetService("ReplicatedStorage")
                        for _, v in pairs(ReplicatedStorage:GetDescendants()) do
                            if v:IsA("RemoteEvent") and (v.Name:lower():find("event") or v.Name:lower():find("join")) then
                                v:FireServer("Summer")
                                break
                            end
                        end
                    end)
                end
            end)
        end
    end
})

AutoFarmTab:CreateToggle({
    Name = "🌱 Auto Plant Plot",
    Default = false,
    Callback = function(state)
        autoPlant = state
        if state then
            task.spawn(function()
                while autoPlant and task.wait(1) do
                    pcall(function()
                        local ReplicatedStorage = game:GetService("ReplicatedStorage")
                        for _, v in pairs(ReplicatedStorage:GetDescendants()) do
                            if v:IsA("RemoteEvent") and (v.Name:lower():find("plant") or v.Name:lower():find("plot")) then
                                v:FireServer()
                                break
                            end
                        end
                    end)
                end
            end)
        end
    end
})

AutoFarmTab:CreateToggle({
    Name = "🐾 Auto Pet Plot",
    Default = false,
    Callback = function(state)
        autoPet = state
        if state then
            task.spawn(function()
                while autoPet and task.wait(1) do
                    pcall(function()
                        local ReplicatedStorage = game:GetService("ReplicatedStorage")
                        for _, v in pairs(ReplicatedStorage:GetDescendants()) do
                            if v:IsA("RemoteEvent") and (v.Name:lower():find("pet") or v.Name:lower():find("place")) then
                                v:FireServer()
                                break
                            end
                        end
                    end)
                end
            end)
        end
    end
})

AutoFarmTab:CreateToggle({
    Name = "🔄 Auto Rebirth",
    Default = false,
    Callback = function(state)
        autoRebirth = state
        if state then
            task.spawn(function()
                while autoRebirth and task.wait(3) do
                    pcall(function()
                        local ReplicatedStorage = game:GetService("ReplicatedStorage")
                        for _, v in pairs(ReplicatedStorage:GetDescendants()) do
                            if v:IsA("RemoteEvent") and (v.Name:lower():find("rebirth") or v.Name:lower():find("prestige")) then
                                v:FireServer()
                                break
                            end
                        end
                    end)
                end
            end)
        end
    end
})

SettingsTab:CreateToggle({
    Name = "🛡️ Anti AFK",
    Default = false,
    Callback = function(state)
        antiAFK = state
        if state then
            task.spawn(function()
                while antiAFK and task.wait(60) do
                    pcall(function()
                        local VirtualUser = game:GetService("VirtualUser")
                        VirtualUser:CaptureController()
                        VirtualUser:ClickButton2(Vector2.new())
                    end)
                end
            end)
        end
    end
})

SettingsTab:CreateToggle({
    Name = "🎬 Hide Egg Animation",
    Default = false,
    Callback = function(state)
        pcall(function()
            local player = game.Players.LocalPlayer
            local eggAnim = player.PlayerGui:FindFirstChild("EggAnimation")
            if eggAnim then
                eggAnim.Enabled = not state
            end
        end)
    end
})

SettingsTab:CreateDropdown({
    Name = "Theme Change",
    Default = "Ocean",
    Options = {"Classic", "Assassin", "Cyberpunk", "Matrix", "Ocean", "Gold"},
    Callback = function(theme)
        Window:SetTheme(theme)
    end
})

print("========================================")
print("✅ qvirthh Hub | PS99 Loaded Successfully!")
print("========================================")

task.spawn(function()
    while task.wait(60) do
        pcall(function()
            local VirtualUser = game:GetService("VirtualUser")
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    end
end)
